{
  "name": "spot-teaser",
  "private": true,
  "type": "module",
  "description": "Renders the 15 s Spot. teaser: frames from teaser/index.html, score from audio.mjs.",
  "scripts": {
    "render": "node render.mjs",
    "stills": "node render.mjs --stills"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright-core": "^1.56.0"
  }
}
